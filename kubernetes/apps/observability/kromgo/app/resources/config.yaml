---
# yaml-language-server: $schema=https://raw.githubusercontent.com/kashalls/kromgo/main/config.schema.json
badge:
  font: Verdana.ttf
  size: 12

metrics:
  - name: k3s_version
    query: kube_node_info
    label: kubelet_version
    title: K3S

  - name: linux_version
    query: label_replace(node_uname_info{kubernetes_node!=""}, "version", "$1", "release", "([0-9]+\\.[0-9]+\\.[0-9]+).*")
    label: version
    title: Linux

  - name: flux_version
    query: label_replace(flux_instance_info, "revision", "$1", "revision", "v(.+)@sha256:.+")
    label: revision
    title: Flux

  - name: cluster_node_count
    query: count(count by (node) (kube_node_status_condition{condition="Ready"}))
    colors:
      - { color: "#a6e3a1", min: 0, max: 9999 }
    title: Nodes

  - name: cluster_pod_count
    query: sum(kube_pod_status_phase{phase="Running"})
    colors:
      - { color: "#a6e3a1", min: 0, max: 9999 }
    title: Pods

  - name: cluster_cpu_usage
    query: round(avg(instance:node_cpu_utilisation:rate5m{kubernetes_node!=""}) * 100, 0.1)
    suffix: "%"
    colors:
      - { color: "#a6e3a1", min: 0, max: 35 }
      - { color: "#fab387", min: 36, max: 75 }
      - { color: "#f38ba8", min: 76, max: 9999 }
    title: CPU

  - name: cluster_memory_usage
    query: round(sum(node_memory_MemTotal_bytes{kubernetes_node!=""} - node_memory_MemAvailable_bytes{kubernetes_node!=""}) / sum(node_memory_MemTotal_bytes{kubernetes_node!=""}) * 100, 0.1)
    suffix: "%"
    colors:
      - { color: "#a6e3a1", min: 0, max: 80 }
      - { color: "#fab387", min: 80, max: 85 }
      - { color: "#f38ba8", min: 85, max: 9999 }
    title: Memory

  - name: cluster_age_days
    query: round((time() - min(kube_node_created) ) / 86400)
    suffix: "d"
    colors:
      - { color: "#a6e3a1", min: 0, max: 180 }
      - { color: "#fab387", min: 181, max: 360 }
      - { color: "#f38ba8", min: 361, max: 9999 }
    title: Age

  - name: cluster_uptime_days
    query: round(avg(node_time_seconds{kubernetes_node!=""} - node_boot_time_seconds{kubernetes_node!=""}) / 86400)
    suffix: "d"
    colors:
      - { color: "#a6e3a1", min: 0, max: 180 }
      - { color: "#fab387", min: 181, max: 360 }
      - { color: "#f38ba8", min: 361, max: 9999 }
    title: Uptime

  - name: cluster_alert_count
    query: alertmanager_alerts{state="active"} - 1 # Ignore Watchdog
    colors:
      - { color: "#a6e3a1", min: 0, max: 0 }
      - { color: "#f38ba8", min: 1, max: 9999 }
    title: Alerts
