---
originRequest:
  originServerName: "external.${SECRET_DOMAIN}"

ingress:
  - hostname: "*.${SECRET_DOMAIN}"
    service: http://cilium-gateway-external.kube-system.svc.cluster.local
  - service: http_status:404
