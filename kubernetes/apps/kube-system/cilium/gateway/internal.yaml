---
# yaml-language-server: $schema=https://github.com/datreeio/CRDs-catalog/raw/refs/heads/main/gateway.networking.k8s.io/gateway_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: internal
  annotations:
    external-dns.alpha.kubernetes.io/target: "internal.popov.wtf"
spec:
  gatewayClassName: cilium
  addresses:
    - type: IPAddress
      value: "10.101.0.202"
  infrastructure:
    annotations:
      external-dns.alpha.kubernetes.io/hostname: "internal.popov.wtf"
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: "*.popov.wtf"
      allowedRoutes:
        namespaces:
          from: Same
    - name: https
      protocol: HTTPS
      port: 443
      hostname: "*.popov.wtf"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        certificateRefs:
          - kind: Secret
            name: popov-wtf-production-tls
